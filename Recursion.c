// Recursion in C

// Factorials in C
/*
#include<stdio.h>
int main()
{
    int i,fact=1,number;
    printf("Enter a number: ");
    scanf("%d",&number);
    for(i=1;i<=number;i++) // for ex- number = 5 and it is < 1 and condition is true! Statement is true!
    {
        fact=fact*i; // fact = 1, i = will be incremented
    }
  printf("Factorial of %d is: %d",number,fact);
    return 0;
}
*/

//

//

//
